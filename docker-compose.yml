version: "3.9"

services:
  app:
    build: .
    container_name: egx-analyzer
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - EGX_HOST=0.0.0.0
      - EGX_PORT=8000
